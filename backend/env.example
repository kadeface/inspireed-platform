# 应用配置
PROJECT_NAME=InspireEd
VERSION=1.0.0
API_V1_STR=/api/v1

# 安全配置
SECRET_KEY=your-secret-key-change-in-production-use-openssl-rand-hex-32
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# CORS - 允许局域网访问
# 格式：逗号分隔的URL列表，或JSON数组格式
# 示例1（逗号分隔）：BACKEND_CORS_ORIGINS=http://localhost:5173,http://192.168.1.100:5173
# 示例2（JSON格式）：BACKEND_CORS_ORIGINS=["http://localhost:5173","http://192.168.1.100:5173"]
# 局域网访问时，请将 192.168.1.100 替换为运行服务器的实际IP地址
# BACKEND_CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# 数据库配置
POSTGRES_SERVER=localhost
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=inspireed
POSTGRES_PORT=5432

# Redis配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# MinIO配置
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=inspireed
MINIO_SECURE=false

# Kafka配置
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
KAFKA_LESSON_LOGS_TOPIC=lesson_logs
KAFKA_QA_LOGS_TOPIC=qa_logs

# JupyterHub配置
JUPYTERHUB_URL=http://localhost:8000
JUPYTERHUB_API_TOKEN=your-jupyterhub-token

# OpenAI配置
OPENAI_API_KEY=sk-your-openai-api-key
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-3.5-turbo
DEFAULT_AI_MODEL=gpt-3.5-turbo
AI_MAX_TOKENS=1000
AI_TEMPERATURE=0.7

# 超级管理员
FIRST_SUPERUSER=admin@inspireed.com
FIRST_SUPERUSER_PASSWORD=admin123

# 资源URL配置
# 资源文件的相对路径前缀（数据库存储的是文件名，路径前缀在配置中）
RESOURCE_BASE_PATH=/uploads/resources
# 资源文件的完整基础URL（可选，不配置则从请求中自动获取）
# 例如：http://192.168.2.35:8000 或 http://localhost:8000
# 如果为空，则从 Request.base_url 自动获取
# RESOURCE_BASE_URL=http://localhost:8000

