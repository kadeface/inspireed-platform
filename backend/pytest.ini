[pytest]
# Pytest 配置文件
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto

# 手跑诊断脚本在 scripts/，不纳入套件
addopts =
    --ignore=scripts/
    -v
